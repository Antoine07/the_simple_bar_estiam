<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="#">The Simple Bar</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a 
                class="{% if routeName == 'home' %}active{% endif %} nav-link"
                href="{{ path('home') }}">Home</a>
            </li>
            {# gestion des catégories #}
               {% for category in categories %}
            <li class="nav-item">
                <a 
                class="{% if 
                routeName == 'category_beer' 
                and category.id  == id
                %}active{% endif %} nav-link" 
                href="{{ path('category_beer', {'id' : category.id} ) }}">{{category.name}}
                </a>
            </li>
            {% endfor %}
        </ul>
    </div>
</nav>